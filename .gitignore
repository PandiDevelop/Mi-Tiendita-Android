# Dependencias de Node (Capacitor CLI, etc.)
node_modules/

# Copia generada del build web (se regenera con: npm run copy:web)
www/

# Logs y basura del sistema
*.log
.DS_Store
Thumbs.db

# graphify: igual que en Web, no versionar cache ni costos
graphify-out/cache/
graphify-out/cost.json
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
