{
  "name": "mi-tiendita-android",
  "version": "1.15.9",
  "private": true,
  "description": "App Android de Mi Tiendita (Capacitor). El codigo web vive en ../Web y solo se copia su build a www/.",
  "scripts": {
    "copy:web": "node scripts/copy-web.mjs",
    "sync": "npm run copy:web && cap sync android",
    "publish": "node scripts/publish.mjs",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.2",
    "@capgo/capacitor-updater": "^8.51.20"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
